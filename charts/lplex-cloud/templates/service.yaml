apiVersion: v1
kind: Service
metadata:
  name: {{ include "lplex-cloud.fullname" . }}
  labels:
    {{- include "lplex-cloud.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lplex-cloud.selectorLabels" . | nindent 4 }}
