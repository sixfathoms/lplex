# v0.1.2

Released February 28, 2026. [GitHub Release](https://github.com/sixfathoms/lplex/releases/tag/v0.1.2)

Release infrastructure fixes only.

## Changes

- Split release artifacts: `lplex` server is Linux-only (requires SocketCAN), `lplexdump` client ships for Linux + macOS
- Homebrew formula (`sixfathoms/tap/lplexdump`) now ships the client only
- Fix Homebrew formula directory placement
